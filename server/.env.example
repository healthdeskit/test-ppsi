# Copy to .env (or use the committed server/.env for local dev only)
PORT=3000
# Admin login at /admin — must match what you type
ADMIN_PASSWORD=your-secure-admin-password
SESSION_SECRET=your-session-secret-string
