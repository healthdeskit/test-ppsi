# Render Blueprint – PPSI auto deploy from GitHub
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: ppsi-web
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: ADMIN_PASSWORD
        sync: false
      - key: SESSION_SECRET
        sync: false
    disks:
      - name: ppsi-data
        mountPath: /app/data
        sizeGB: 1
    # Starter (or higher) is recommended for persistent health and no sleep.
    plan: starter
